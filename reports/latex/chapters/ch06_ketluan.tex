\chapter{KẾT LUẬN}

\section{Tóm Tắt Đề Tài}

Đề tài nghiên cứu bài toán phân loại nhị phân ảnh khuôn mặt thật và ảnh khuôn mặt do AI tổng hợp --- một vấn đề kỹ thuật có ý nghĩa xã hội cấp thiết trong bối cảnh các công cụ sinh ảnh bằng GAN và diffusion models ngày càng tạo ra nội dung photorealistic khó phân biệt bằng mắt thường. Đề tài xây dựng pipeline hoàn chỉnh từ thu thập và hợp nhất bốn bộ dữ liệu Kaggle đa dạng (316.530 ảnh sau dedup) đến huấn luyện mô hình EfficientNet-B0 \cite{tan2019efficientnet} theo chiến lược fine-tuning 3 giai đoạn (progressive unfreezing), đạt Accuracy 97,33\%, F1-Score 97,45\% và AUC-ROC 99,70\% trên tập kiểm tra độc lập. Ngoài đánh giá chuẩn, đề tài thiết kế thêm bài kiểm tra cross-generator để đo khả năng tổng quát hóa, bài kiểm tra robustness để đánh giá độ bền dưới nhiễu thực tế, và trực quan hóa Grad-CAM \cite{selvaraju2017gradcam} để giải thích cơ chế quyết định của mô hình.

\section{Đóng Góp Của Đề Tài}

Đề tài thực hiện năm đóng góp cụ thể:

\begin{enumerate}
  \item \textbf{Pipeline dữ liệu đa nguồn quy mô lớn:} Xây dựng quy trình hợp nhất bốn bộ dữ liệu Kaggle với dedup MD5 và stratified split 70/15/15, tạo tập huấn luyện 221.571 ảnh đa dạng hơn so với các nghiên cứu thường sử dụng một nguồn đơn lẻ.

  \item \textbf{Chiến lược huấn luyện 3 giai đoạn có kiểm soát:} Áp dụng progressive unfreezing (Head-only $\to$ Partial unfreeze blocks 5--6 $\to$ Full fine-tune) kết hợp EarlyStopping và ReduceLROnPlateau, với siêu tham số quản lý tập trung qua \texttt{train\_config.yaml}. Val accuracy cải thiện rõ rệt: 86,63\% $\to$ 94,54\% $\to$ 97,34\%.

  \item \textbf{Đánh giá cross-generator có hệ thống:} Thực hiện hold-out evaluation theo từng bộ dữ liệu nguồn để định lượng khả năng tổng quát hóa thực tế --- quan trọng hơn benchmark in-distribution thông thường.

  \item \textbf{Đánh giá robustness với nhiễu thực tế:} Kiểm tra JPEG compression ($q = 70/50/30$), downscale (50\%/25\%) và Gaussian blur ($\sigma = 1/2$), phản ánh điều kiện ảnh phổ biến khi chia sẻ qua mạng xã hội.

  \item \textbf{Explainability qua Grad-CAM:} Cung cấp bằng chứng trực quan về vùng ảnh mô hình tập trung khi phân loại, tăng tính minh bạch và hỗ trợ tin cậy hóa hệ thống AI trong ứng dụng thực tế.
\end{enumerate}

\section{Bài Học Kinh Nghiệm}

Quá trình thực hiện đề tài rút ra một số bài học kinh nghiệm có giá trị cho các dự án deep learning tương tự:

\begin{table}[H]
  \centering
  \caption{Bài học kinh nghiệm từ quá trình thực hiện đề tài}
  \label{tab:lessons}
  \begin{tabularx}{\linewidth}{lX}
    \toprule
    \textbf{Lĩnh vực} & \textbf{Bài học} \\
    \midrule
    Dữ liệu & MD5 dedup là bước bắt buộc khi hợp nhất nhiều nguồn Kaggle --- các bộ dữ liệu công khai thường có phần chồng lấp không được ghi rõ trong tài liệu, gây rò rỉ dữ liệu tiềm ẩn. \\
    Huấn luyện & Với tập >200k ảnh và VRAM 12GB, batch\_size=128 + FP16 là điểm cân bằng tốt nhất; tăng lên 256 gây OOM, giảm xuống 64 làm gradient estimate kém ổn định hơn. \\
    Đánh giá & AUC-ROC tin cậy hơn Accuracy khi phân bố nhãn không hoàn toàn cân bằng; luôn báo cáo cả hai. Cross-generator evaluation cung cấp thông tin thực tế hơn in-distribution test. \\
    Explainability & Grad-CAM là công cụ gỡ lỗi hiệu quả: heatmap phân tán trên ciplab chỉ ngay ra vấn đề domain mismatch trước khi cần phân tích số liệu chi tiết. \\
    Reproducibility & Quản lý siêu tham số qua YAML + seed cố định là đầu tư nhỏ nhưng tiết kiệm nhiều thời gian khi cần tái chạy hoặc so sánh cấu hình khác nhau. \\
    \bottomrule
  \end{tabularx}
\end{table}

\section{Hạn Chế Của Đề Tài}

Đề tài có một số hạn chế cần được thừa nhận rõ ràng để định hướng đúng cho các nghiên cứu tiếp theo:

\begin{enumerate}
  \item \textbf{Thiếu ảnh từ diffusion models thế hệ mới:} Tập dữ liệu không bao gồm ảnh từ Stable Diffusion, Midjourney, DALL-E~3 --- các generator này ngày càng phổ biến và tạo ra artifact khác GAN về cấu trúc tần số. Kết quả đề tài không thể suy luận sang các generator này.

  \item \textbf{Sụp đổ hoàn toàn trên ciplab holdout:} AUC-ROC 53,44\% khi holdout ciplab là điểm yếu nghiêm trọng. Nguyên nhân gốc là ciplab GAN tạo ra phân bố artifact nằm hoàn toàn ngoài vùng học được từ ba nguồn còn lại --- giải pháp cần bổ sung dữ liệu ciplab vào tập huấn luyện.

  \item \textbf{Nhạy cảm với nén JPEG:} Suy giảm $\approx 15$ điểm phần trăm accuracy tại JPEG $q=70$ đáng lo ngại cho triển khai trên ảnh chia sẻ qua mạng xã hội. Cần bổ sung \texttt{ImageCompression} augmentation.

  \item \textbf{Chỉ xử lý ảnh tĩnh:} Video deepfake --- trường hợp sử dụng phổ biến và nguy hiểm hơn --- nằm ngoài phạm vi đề tài.

  \item \textbf{EarlyStopping không kích hoạt:} Tất cả ba giai đoạn chạy hết 30 epochs, gợi ý mô hình có thể cải thiện thêm nếu tăng \texttt{max\_epochs\_per\_stage}.
\end{enumerate}
